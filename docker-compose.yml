services:
  jekyll:
    image: jekyll-ubuntu:latest
    command: jekyll serve --host 0.0.0.0 --watch --force_polling
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
    working_dir: /srv/jekyll